# 🐳 คู่มือการใช้งาน Docker (Docker Guide)

คู่มือนี้จะแนะนำวิธีการรันโปรเจกต์นี้ด้วย Docker เพื่อความสะดวกและรวดเร็ว โดยไม่ต้องติดตั้ง PHP หรือ MySQL ลงในเครื่องโดยตรง

## 📋 สิ่งที่ต้องมี (Prerequisites)

1.  **Docker Desktop**: ต้องติดตั้ง Docker Desktop ก่อน (ดาวน์โหลดได้ที่ [docker.com](https://www.docker.com/products/docker-desktop/))
2.  **Git**: สำหรับ Clone โปรเจกต์ (ถ้ายังไม่มี)

---

## 🚀 วิธีการรัน (How to Run)

1.  **เปิด Terminal** (เช่น PowerShell, Command Prompt หรือ Git Bash)
2.  **ไปที่โฟลเดอร์โปรเจกต์**:
    ```bash
    cd path/to/your/project
    ```
3.  **สั่งรัน Docker Compose**:

    ```bash
    docker-compose up -d --build
    ```

    _คำสั่งนี้จะทำการสร้าง (build) และเริ่มทำงาน (start) container ทั้งหมดใน background_

4.  **รอสักครู่**: ครั้งแรกอาจใช้เวลาหน่อยในการดาวน์โหลด Image และติดตั้ง Database

---

## 🌐 การเข้าใช้งาน (Accessing the App)

เมื่อรันเสร็จแล้ว สามารถเข้าใช้งานได้ที่:

👉 **http://localhost:8080**

---

## 🗄️ ข้อมูลฐานข้อมูล (Database Info)

ระบบจะสร้างฐานข้อมูลให้อัตโนมัติโดยใช้ข้อมูลดังนี้:

- **Host**: `db`
- **Database**: `internship_system`
- **Username**: `root`
- **Password**: `rootpassword`
- **Port**: `3306` (ภายใน Container)

_หมายเหตุ: ไฟล์ `config/database.php` ได้ถูกปรับแก้ให้รองรับทั้งแบบ Docker และแบบ XAMPP ปกติแล้ว_

---

## 🛑 วิธีการหยุด (Stopping)

เมื่อต้องการเลิกใช้งาน ให้รันคำสั่ง:

```bash
docker-compose down
```

---

## 🛠️ การแก้ไขปัญหาเบื้องต้น (Troubleshooting)

### 1. Port 8080 ไม่ว่าง (Port conflict)

ถ้าเครื่องของคุณมีการใช้ Port 8080 อยู่แล้ว (เช่น Tomcat หรืออื่นๆ) ให้แก้ไขไฟล์ `docker-compose.yml`:

```yaml
ports:
  - "8081:80"  <-- เปลี่ยนเลขหน้าเป็น Port ที่ว่าง เช่น 8081
```

แล้วเข้าใช้งานที่ `http://localhost:8081` แทน

### 2. ฐานข้อมูลไม่เชื่อมต่อ

ถ้ารันครั้งแรกแล้วขึ้น Error เกี่ยวกับ Database ให้ลองรอสักพัก (1-2 นาที) แล้วรีเฟรชหน้าเว็บ เพราะ MySQL อาจจะยัง Boot ไม่เสร็จ

### 3. ต้องการ Reset ฐานข้อมูลใหม่หมด

หากต้องการลบข้อมูลทั้งหมดและเริ่มใหม่:

```bash
docker-compose down -v
docker-compose up -d
```

_(คำเตือน: ข้อมูลทั้งหมดใน Database จะหายไป)_
