# 🎯 คำแนะนำการใช้งานระบบใหม่

## ✨ ฟีเจอร์ที่เพิ่มเข้ามา:

### 1. **ปุ่มอัพเดทสถานะ 3 ประเภท** (สำหรับ Admin)
   - 🔵 **ผลการพิจารณาของคณะ** - Admin คลิกอนุมัติ/ไม่อนุมัติ
   - 🟢 **ผลการตอบรับจากหน่วยงาน** - Admin คลิกตอบรับ/ปฏิเสธ
   - 🟣 **ผลการตอบกลับเอกสาร** - Admin คลิกส่งแล้ว/อนุมัติ/ไม่อนุมัติ

### 2. **ฟีเจอร์ปริ้นเอกสาร** 🖨️
   - ปริ้นหนังสือขอความอนุเคราะห์ให้นิสิตฝึกประสบการณ์วิชาชีพ
   - ใช้รูปแบบเดียวกับโค้ดเดิมของคุณ

### 3. **แสดงผลในหน้า Dashboard ของนิสิต**
   - นิสิตจะเห็นสถานะทั้ง 3 ประเภทของตัวเอง

---

## 📋 ขั้นตอนการติดตั้ง:

### ขั้นที่ 1: อัพเดทฐานข้อมูล ⚙️

รันคำสั่ง SQL นี้ใน phpMyAdmin:

```sql
USE internship_system;

-- สร้างตาราง internship_rounds
CREATE TABLE IF NOT EXISTS `internship_rounds` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `round_name` varchar(100) NOT NULL,
  `year` int(4) NOT NULL,
  `start_date` date NOT NULL,
  `end_date` date NOT NULL,
  `is_active` tinyint(1) DEFAULT 1,
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- เพิ่มข้อมูลตัวอย่าง
INSERT IGNORE INTO `internship_rounds` (`round_name`, `year`, `start_date`, `end_date`, `is_active`) VALUES
('รอบที่ 1/2567', 2024, '2024-06-01', '2024-08-31', 1),
('รอบที่ 2/2567', 2024, '2024-11-01', '2025-01-31', 1),
('รอบที่ 1/2568', 2025, '2025-06-01', '2025-08-31', 1);

-- เพิ่มคอลัมน์ใหม่ (รันทีละคำสั่ง ถ้า error ให้ข้าม)
ALTER TABLE internship_requests ADD COLUMN faculty_approval_status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending';
ALTER TABLE internship_requests ADD COLUMN faculty_approval_date DATETIME NULL;
ALTER TABLE internship_requests ADD COLUMN faculty_comment TEXT NULL;
ALTER TABLE internship_requests ADD COLUMN company_response_status ENUM('pending', 'accepted', 'rejected') DEFAULT 'pending';
ALTER TABLE internship_requests ADD COLUMN company_response_date DATETIME NULL;
ALTER TABLE internship_requests ADD COLUMN company_response_comment TEXT NULL;
ALTER TABLE internship_requests ADD COLUMN document_response_status ENUM('pending', 'submitted', 'approved', 'rejected') DEFAULT 'pending';
ALTER TABLE internship_requests ADD COLUMN document_response_date DATETIME NULL;
ALTER TABLE internship_requests ADD COLUMN document_files TEXT NULL;
ALTER TABLE internship_requests ADD COLUMN round_id INT NULL;
ALTER TABLE internship_requests ADD COLUMN updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;
```

### ขั้นที่ 2: เพิ่ม Script Tag 📜

เปิดไฟล์ `src/Admin/internship_requests.php`

ค้นหาบรรทัดที่มี `</body>` แล้วเพิ่มบรรทัดนี้ก่อนหน้ามัน:

```html
<script src="assets/js/admin_requests.js"></script>
</body>
```

### ขั้นที่ 3: ตรวจสอบไฟล์ที่สร้างแล้ว ✅

ตรวจสอบว่าไฟล์เหล่านี้มีอยู่:
- ✅ `assets/js/admin_requests.js`
- ✅ `src/Admin/process_update_status.php`
- ✅ `update_internship_requests_table.sql`
- ✅ `SQL_INSTALLATION_GUIDE.md`

---

## 🎮 วิธีใช้งาน:

### สำหรับ Admin:

1. **เข้าสู่ระบบในฐานะ Admin**
2. **ไปที่หน้า "อนุมัติคำขออฝึกงาน"**
3. **คลิกปุ่ม "ดู" 👁️** ที่คำขอใดๆ
4. **Modal จะเปิดขึ้นมา** แสดง:
   - ข้อมูลนิสิต
   - ข้อมูลบริษัท/หน่วยงาน
   - **3 การ์ดสำหรับอัพเดทสถานะ:**
     - 🔵 ผลการพิจารณาของคณะ (ปุ่ม: อนุมัติ / ไม่อนุมัติ)
     - 🟢 ผลการตอบรับจากหน่วยงาน (ปุ่ม: ตอบรับ / ปฏิเสธ)
     - 🟣 ผลการตอบกลับเอกสาร (ปุ่ม: ส่งแล้ว / อนุมัติ / ไม่อนุมัติ)
   - สถานะคำขอหลัก (ปุ่ม: อนุมัติคำขอ / ไม่อนุมัติ)

5. **คลิกปุ่ม "ปริ้นเอกสาร" 🖨️** เพื่อพิมพ์หนังสือขอความอนุเคราะห์

### การอัพเดทสถานะ:

1. **คลิกปุ่มในการ์ดที่ต้องการ**
2. **ยืนยันการเปลี่ยนแปลง**
3. **ระบบจะอัพเดทและรีเฟรชหน้าอัตโนมัติ**

---

## 📊 การแสดงผลในหน้า Dashboard ของนิสิต:

นิสิตจะเห็น:
- ✅ ผลการพิจารณาของคณะ
- ✅ ผลการตอบรับจากหน่วยงาน
- ✅ ผลการตอบกลับเอกสาร
- ✅ สถานะคำขอหลัก

*(หมายเหตุ: ต้องแก้ไขหน้า Dashboard ของนิสิตเพิ่มเติม)*

---

## 🎨 ภาพรวมระบบ:

```
Admin คลิกปุ่ม → อัพเดทสถานะ → บันทึกลงฐานข้อมูล → แสดงผลในหน้านิสิต
```

### ตัวอย่างการทำงาน:

1. **Admin เปิด Modal** → เห็นสถานะทั้งหมด
2. **Admin คลิก "อนุมัติ"** ในการ์ด "ผลการพิจารณาของคณะ"
3. **ระบบอัพเดท** `faculty_approval_status = 'approved'`
4. **นิสิตเข้าดู Dashboard** → เห็นว่าคณะอนุมัติแล้ว ✅

---

## 🔧 การแก้ไขปัญหา:

### ถ้าปุ่มไม่ทำงาน:
1. ตรวจสอบว่าเพิ่ม `<script src="assets/js/admin_requests.js"></script>` แล้ว
2. เปิด Console ใน Browser (F12) ดู error
3. ตรวจสอบว่าไฟล์ `admin_requests.js` อยู่ในโฟลเดอร์ที่ถูกต้อง

### ถ้า SQL Error:
1. ตรวจสอบว่ารันคำสั่ง SQL ครบทุกคำสั่งแล้ว
2. ตรวจสอบว่าตาราง `internship_rounds` ถูกสร้างแล้ว
3. รันคำสั่ง `DESCRIBE internship_requests;` เพื่อดูโครงสร้างตาราง

### ถ้าปริ้นไม่ออก:
1. ตรวจสอบว่า Browser อนุญาตให้เปิด popup
2. ตรวจสอบว่ามีรูป `../img/garuda.png` หรือไม่

---

## 📝 หมายเหตุสำคัญ:

- ✅ ระบบนี้รองรับนิสิต **10,000+ คน**
- ✅ เฉพาะ**นิสิตปี 3** เท่านั้นที่สามารถฝึกงานได้
- ✅ แบ่งตามสาขาวิชาต่างๆ
- ✅ **ไม่มีปุ่มส่งออก Excel** ในหน้านี้ (ตามที่คุณต้องการ)
- ✅ ฟีเจอร์ปริ้นเอกสารใช้รูปแบบเดิมของคุณ

---

## 🚀 ขั้นตอนถัดไป (ถ้าต้องการ):

1. แก้ไขหน้า Dashboard ของนิสิตให้แสดงสถานะทั้ง 3 ประเภท
2. เพิ่มระบบแจ้งเตือนผ่านอีเมลเมื่อสถานะเปลี่ยน
3. เพิ่มระบบอัพโหลดเอกสารแนบ
4. เพิ่มระบบ Comment/ความคิดเห็นในแต่ละสถานะ

---

**สร้างโดย:** ระบบจัดการฝึกงาน  
**วันที่:** 2025-11-22  
**เวอร์ชัน:** 2.0

---

## 💡 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือข้อสงสัย:
1. Screenshot หน้าจอ Error
2. Copy ข้อความ Error จาก Console (F12)
3. บอกว่ากำลังทำขั้นตอนไหนอยู่
